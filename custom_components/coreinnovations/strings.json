{
  "config": {
    "flow_title": "{name}",
    "step": {
      "user": {
        "description": "Choose a Core Innovations CTP500 to set up",
        "data": {
          "address": "Device",
          "speed": "Print speed (lower = faster)",
          "energy": "Darkness / energy",
          "feed": "Feed after print (pixels)",
          "packet_delay": "Delay between BLE writes (ms)",
          "chunk_size": "BLE write chunk size (bytes)",
          "scan_interval": "Scan interval (seconds)",
          "problem_feeding": "Feed by drawing blank lines (for printers that ignore the feed command)",
          "keep_connection": "Keep BLE connection open"
        }
      },
      "bluetooth_confirm": {
        "description": "Do you want to set up {name}?",
        "data": {
          "speed": "Print speed (lower = faster)",
          "energy": "Darkness / energy",
          "feed": "Feed after print (pixels)",
          "packet_delay": "Delay between BLE writes (ms)",
          "chunk_size": "BLE write chunk size (bytes)",
          "scan_interval": "Scan interval (seconds)",
          "problem_feeding": "Feed by drawing blank lines (for printers that ignore the feed command)",
          "keep_connection": "Keep BLE connection open"
        }
      }
    },
    "abort": {
      "no_devices_found": "No Core Innovations CTP500 printers found",
      "already_in_progress": "Configuration flow is already in progress",
      "already_configured": "Device is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "description": "Tune printing for your CTP500",
        "data": {
          "speed": "Print speed (lower = faster)",
          "energy": "Darkness / energy",
          "feed": "Feed after print (pixels)",
          "packet_delay": "Delay between BLE writes (ms)",
          "chunk_size": "BLE write chunk size (bytes)",
          "scan_interval": "Scan interval (seconds)",
          "problem_feeding": "Feed by drawing blank lines (for printers that ignore the feed command)",
          "keep_connection": "Keep BLE connection open"
        }
      }
    }
  }
}
